// Test generated by RoostGPT for test dm-example-4 using AI Model gpt

package com.example.exception;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class ErrorResponse_setErrorCodeac0de600a7Test {

    private ErrorResponse errorResponse;

    @BeforeEach
    public void setUp() {
        errorResponse = Mockito.mock(ErrorResponse.class);
    }

    @Test
    public void testSetErrorCode_Success() {
        int expectedErrorCode = 200;
        Mockito.doAnswer(invocation -> {
            Integer arg = invocation.getArgument(0);
            assertEquals(expectedErrorCode, arg);
            return null;
        }).when(errorResponse).setErrorCode(Mockito.anyInt());

        errorResponse.setErrorCode(expectedErrorCode);
    }

    @Test
    public void testSetErrorCode_Failure() {
        int unexpectedErrorCode = 500;
        int actualErrorCode = 400;
        Mockito.doAnswer(invocation -> {
            Integer arg = invocation.getArgument(0);
            assertEquals(unexpectedErrorCode, arg);
            return null;
        }).when(errorResponse).setErrorCode(Mockito.anyInt());

        errorResponse.setErrorCode(actualErrorCode);
    }
}
