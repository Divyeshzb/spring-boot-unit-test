# Test generated by RoostGPT for test dm-example-5 using AI Model gpt

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/todo"
          expect:
            - statusCode: 200
            - hasProperty: "ToDo"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/todo"
          beforeRequest:
            js:
              function(context, ee, next) {
                context.vars.missingField = '';
                return next();
              }
          expect:
            - statusCode: 400
            - body: "Missing required field"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/todo"
          beforeRequest:
            js:
              function(context, ee, next) {
                context.vars.largeData = 'a'.repeat(1024 * 1024);
                return next();
              }
          expect:
            - statusCode: "{{ 200..599 }}"
            - body: "{{ regex('\\A\\s*\\Z') }}"
